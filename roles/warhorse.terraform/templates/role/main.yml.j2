---
- ansible.builtin.include_tasks: deploy.yml
  vars:
    state: "present"
    name: "Deploy"
  when: deploy

- ansible.builtin.include_tasks: destroy.yml
  vars:
    state: "absent"
    name: "Destroy"
  when: not deploy
